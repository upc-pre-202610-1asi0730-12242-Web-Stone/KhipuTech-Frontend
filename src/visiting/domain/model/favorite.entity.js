export class Favorite {
    constructor(id, artworkId, sessionId, userId, createdAt) {
        this.id = id
        this.artworkId = artworkId
        this.sessionId = sessionId
        this.userId = userId
        this.createdAt = createdAt
    }
}